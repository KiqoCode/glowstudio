import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'se.createbymattis.app',
  appName: 'createbymattis',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
